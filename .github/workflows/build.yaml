name: build
on:
  workflow_dispatch:
  release:
    types: [ created ]
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: true

      - name: install node
        uses: actions/setup-node@v2
        with:
          node-version: 16

      - name: install rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable

      - name: install dependencies
        run: npm ci

      - name: build frontend
        run: npm run esbuild

      - name: build release app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: app-v__VERSION__
          releaseName: "Storybook Brawl Live v__VERSION__"
          releaseBody: "Release build"
          releaseDraft: false
          prerelease: true

      - name: build debug app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: --debug
          tagName: app-v__VERSION__-debug
          releaseName: "Storybook Brawl Live v__VERSION__ (Debug)"
          releaseBody: "Debug build"
          releaseDraft: true
          prerelease: false
