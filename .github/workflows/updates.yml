name: publish updater

on:
  release:

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: deploy updates.json
        run: |
          echo { \
            "name": "${{ github.release.name }}", \
            "notes": "${{ github.release.body }}", \
            "pub_date": "${{ github.release.published_at }}", \
            "platforms": { \
              "win64": { \
                "signature": "", \
                "url": "${{ github.release.assets[0].browser_download_url }}" \
              } \
            } \
          }