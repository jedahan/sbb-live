{
  "name": "sbb-match-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format": "npm run ts-standard -- --fix",
    "start": "npm run test:watch & npm run serve",
    "serve": "npm run esbuild -- --sourcemap --servedir=build",
    "build": "npm run esbuild -- --minify",

    "test:watch": "npm run tsc -- --watch",
    "test": "npm run tsc && npm run ts-standard",

    "esbuild": "esbuild index.tsx --bundle --loader:.png=file --outfile=build/index.js",
    "ts-standard": "ts-standard",
    "tsc": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/react": "^17.0.38",
    "@types/react-dom": "^17.0.11",
    "esbuild": "^0.14.11",
    "react": "^17.0.2",
    "ts-standard": "^11.0.0",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "react-dom": "^17.0.2"
  }
}
